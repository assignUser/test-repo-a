name: Dev PR
on:
  pull_request:
  
permissions:
  contents: read
  actions: read 

jobs:
  get:
    runs-on: macos-14
    steps:
      - run: |
          ls /usr/local || echo blub
          ls /usr/opt || echo blub
    
          
