name: Dev PR
on:
  workflow_dispatch:
    inputs:
      opt:
        default: "5"
  pull_request:

jobs:
  get:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo ${{ fromJSON("1+1") }}
                    
