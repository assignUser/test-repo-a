name: Dev PR
on:
  workflow_dispatch:
  pull_request:
permissions: 
  contents: read
  
jobs:        
  step1:
    name: Another Name
    runs-on: ubuntu-latest
    steps:
      - run: pgrep -f Runner.Worker
      

                    
