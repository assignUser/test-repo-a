name: Dev PR
on:
  pull_request:
  
permissions:
  contents: read
  actions: read 
env:
  VAR1: "Deploy\" ]; echo \"Injected!\"; ls; echo \"Done ["
jobs:
  get:
    runs-on: ubuntu-latest
    steps:
      - env:
         VAR: ${{ env.VAR1 }}
        run: |
          if [ "${VAR}" == "Deploy" ]; then
            echo blub
          fi
    
          
