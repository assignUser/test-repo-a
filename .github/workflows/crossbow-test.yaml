name: Dev PR
on:
  pull_request:

jobs:
   test-source:
    name: Test
    runs-on: ubuntu-latest
    steps:
      - continue-on-error: true
        id: blub
        run: exit 1
      - run: echo "${{ steps.blub.conclusion }}"
          
