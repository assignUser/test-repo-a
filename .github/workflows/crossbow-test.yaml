name: Dev PR
on:
  workflow_dispatch:
  push:
    paths:
      - .github/workflows/crossbow-test.yaml

jobs:        
  step1:
    runs-on: macos-latest
    steps:
      - uses: r-lib/actions/setup-r@v2
      - shell: Rscript {0}
        run: |
          Sys.info()
        
          
