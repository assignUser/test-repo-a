name: Dev PR
on:
  pull_request:
  
permissions:
  contents: read
  actions: read

jobs:
  get:
    runs-on: ubuntu-22.04
    steps:
        - run: echo "blub=test" >> $GITHUB_ENV
  get2:
    runs-on: ubuntu-22.04
    needs: get
    steps:
        - run: echo $blub
