name: Dev PR
on:
  workflow_dispatch:
  push:
    paths:
      - .github/workflows/crossbow-test.yaml

jobs:        
  macos11:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "$SECOND"
        env:
          FIRST: "THIS IS THE END"
          SECOND: "IS $FIRST?"

