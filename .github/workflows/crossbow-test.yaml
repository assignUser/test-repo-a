name: Dev PR
on:
  #push:
  workflow_dispatch:
  pull_request:

jobs:
   test-source:
    name: Test
    runs-on: ubuntu-20.04
    steps:
      - env:
          TEST: "${{ false && 'true' || ''}}"
        run: |
             #find /usr -iname '*lz4*'
             cat /usr/lib/x86_64-linux-gnu/pkgconfig/liblz4.pc

