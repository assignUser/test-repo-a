name: Dev PR
on:
  pull_request:
  
permissions:
  contents: read
  actions: read

jobs:
  get:
    runs-on: windows-latest
    steps:
      - env:
          VERSION: "20.0.0"
          RC: "0"
        run: echo "apache-arrow-%VERSION%-rc%RC%"
        
