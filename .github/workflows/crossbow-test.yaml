name: Dev PR
on:
  pull_request:

jobs:
  get:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: |
          stat -c "%u:%g $GITHUB_WORKSPACE
          stat -c "%u:%g ~
          stat -c "%u:%g R

