name: Dev PR
on:
  workflow_dispatch:
  pull_request:

jobs:        
  step1:
    name: Another Name
    runs-on: ubuntu-latest-arm
    steps:
      - env:
          ARGS: "false"
        run: |
          echo $ARGS
          export ARGS=true
          arch
          echo $ARGS

          
      

                    
