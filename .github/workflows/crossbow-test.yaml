name: Dev PR
on:
  workflow_dispatch:
  pull_request:

jobs:        
  step1:
    name: Another Name
    runs-on: ubuntu-latest
    steps:
      - env:
          ARGS: "'foo' 'bar' 'baz'"
        run: |
          echo "$ARGS"
          echo $ARGS
          for a in $ARGS; do
            echo $a
          done
      

                    
