name: Dev PR
on:
  #push:
  workflow_dispatch:
  pull_request:

jobs:
   test-source:
    name: Test
    runs-on: macos-12
    steps:
      - run: echo " ${{github.event_name == 'pull_request' && github.head_ref || github.sha }}"


