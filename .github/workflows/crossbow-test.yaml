name: Dev PR
on:
  pull_request:
  
permissions:
  contents: read
  actions: read

jobs:
  get:
    runs-on: ubuntu-latest
    steps:
        - uses: aquasecurity/trivy-action@0.30.0
          with:
            skip-setup-trivy: true
