name: Dev PR
on:
  pull_request:

jobs:
  get:
    runs-on: ubuntu-latest
    steps:
      - env:
          VAR: ${{ false && 'A' || '' }}
        run: |
          echo "var_${VAR}_"
          [ -z "${VAR+x}" ] && echo "unset" || echo set
