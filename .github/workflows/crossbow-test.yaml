name: Dev PR
on:
  workflow_dispatch:
  push:
    paths:
      - .github/workflows/crossbow-test.yaml
      
permissions: 
  contents: read
env:
  A: blub
jobs:        
  blub:
    runs-on: ubuntu-latest
    env:
      B: "some more $A"
      
    steps:
      - run: |
          echo $B
          echo $C
