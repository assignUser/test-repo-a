name: Dev PR
on:
  workflow_dispatch:
  pull_request:

jobs:
  get:
    runs-on: ubuntu-latest
    env:
      VAR1: ""
      VAR2: ${{ format('bla/{0}', github.base_ref) }}
    steps:
      - id: test
        run: |
          echo "bla=false" >> "$GITHUB_OUTPUT"
      - if: ${{ steps.test.outputs.bla }}
        run: echo Shouldnt run
                    
