name: Dev PR
on:
  workflow_dispatch:
  push:
    paths:
      - .github/workflows/crossbow-test.yaml
      
permissions: 
  contents: read
jobs:        
  step1:
    runs-on: ubuntu-latest
    steps:
       - shell: bash
         env:
           PYTEST_ARGS: "-W error -W \"ignore:Setting custom ClientSession:DeprecationWarning\""
         run: echo $PYTEST_ARGS

        
