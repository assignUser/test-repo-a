name: Dev PR
on:
  pull_request:
env:
  opt: 5
jobs:
  get:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        var: ["foo", "bar", "baz"]
        opt: ["${{ env.opt }}"]
        exclude:
          - var: "baz"
            opt: "6"
    if: ${{ matrix.bla == 1 }}
    steps:
      - run: |
          echo ${{ false || false }}
          echo ${{ true || false }}
          echo ${{ false || true }}
          
