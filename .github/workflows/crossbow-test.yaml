name: Dev PR
on:
  pull_request:

jobs:
  get:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo ${{ false || false }}
          echo ${{ true || false }}
          echo ${{ false || true }}
          
