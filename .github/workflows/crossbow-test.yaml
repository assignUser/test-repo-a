name: Dev PR
on:
  workflow_dispatch:
  pull_request:

jobs:        
  step1:
    name: Another Name
    runs-on: ubuntu-latest
    steps:
      - env:
          ARGS: "false"
        run: |
          grep -E 'avx512' /proc/cpuinfo

          
      

                    
