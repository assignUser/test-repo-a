name: Dev PR
on:
  workflow_dispatch:
  push:
    paths:
      - .github/workflows/crossbow-test.yaml

jobs:        
  macos11:
    runs-on: ubuntu-latest
    steps:
      - run: |
          mkdir base
          touch base/test.R
      - uses: actions/checkout@v3
        with:
          path: base
