name: Dev PR
on:
  pull_request:

jobs:
  get:
    runs-on: ubuntu-latest
    steps:
      - run: |
          exit 1
      - env:
          VAR: ${{ false && 'A' || '' }}
        run: |
          echo $VAR
          [ -z "${VAR+x}" ] && echo "unset"
