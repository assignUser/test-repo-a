name: Dev PR
on:
  workflow_dispatch:
  pull_request:
permissions: 
  contents: read
  
jobs:        
  step1:
    name: Another Name
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          persist-credentials: true
      - run: |
          cat .git/config
          
      

                    
