name: Dev PR
on:
  workflow_dispatch:
  pull_request:
permissions: 
  contents: read
  
jobs:        
  step1:
    name: Another Name
    runs-on: ubuntu-latest
    steps:
      - run: |
          mkdir -p a b
          touch a/foo b/bar
          tar --version
          tar -czvf foobar.tar.gz ./a ./b
          ls -la
      

                    
