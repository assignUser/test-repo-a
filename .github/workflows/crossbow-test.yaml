name: Dev PR
on:
  pull_request:
  
permissions:
  contents: read
  actions: read 
env:
  VAR1: "true\" && echo test &&\" echo 5"
jobs:
  get:
    runs-on: ubuntu-latest
    steps:
      - env:
         VAR: ${{ env.VAR1 }}
        run: |
          if [ "${VAR}" -gt 0 ]; then
            echo blub
          fi
    
          
