name: Dev PR
on:
  pull_request:

jobs:
  get:
    strategy:
      fail-fast: false
      matrix:
        os: ["macos-14", "ubuntu-22.04", "ubuntu-24.04-arm"]
    runs-on: ${{ matrix.os }}
    steps:
      - run: |
          pipx install --force cmake==3.31
      - run: cmake --version
        
