name: Dev PR
on:
  pull_request:
  
permissions:
  contents: read
  actions: read

jobs:
  get:
    runs-on: macos-13
    steps:
      - run: |
          pipx install --force cmake==3.31
          cmake --version
        
