name: Dev PR
on:
  pull_request:

jobs:
  get:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        bla: [1, 2]
    if: ${{ matrix.bla == 1 }}
    steps:
      - run: |
          echo ${{ false || false }}
          echo ${{ true || false }}
          echo ${{ false || true }}
          
