name: Dev PR
on:
  pull_request:
  
permissions:
  contents: read
  actions: read

jobs:
  get:
    runs-on: windows-latest
    steps:
      - env:
          VERSION: "20.0.0"
          RC: "0"
        shell: cmd
        run: |
          echo "apache-arrow-%ARROW_VERSION%-rc%RC_NUMBER%"
          set TAG=apache-arrow-%ARROW_VERSION%-rc%RC_NUMBER%
          echo Tag: %TAG%

        
