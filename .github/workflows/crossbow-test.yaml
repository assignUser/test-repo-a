name: Dev PR
on:
  pull_request:
  
permissions:
  contents: read
  actions: read 

jobs:
  get:
    runs-on: macos-13
    steps:
      - run: |
          ls /usr/local || blub
          ls /usr/opt || blub
    
          
