name: Dev PR
on:
  workflow_dispatch:
  push:
    paths:
      - .github/workflows/crossbow-test.yaml
      
permissions: 
  contents: read
jobs:        
  blub:
    runs-on: ubuntu-latest
    env:
      A: blub
      B: "some more $A"
      C: "some more ${{ env.A }}"
    steps:
      - run: |
          echo $B
          echo $C
