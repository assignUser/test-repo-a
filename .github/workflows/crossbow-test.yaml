name: Dev PR
on:
  workflow_dispatch:
    inputs:
      opt:
        default: "5"
  pull_request:

jobs:
  get:
    runs-on: ubuntu-latest
    env:
      VAR1: ""
      VAR2: ${{ github.event.push }}
    steps:
      - run: |
          echo "GITHUB_ACTIONS=$GITHUB_ACTIONS"
                    
