name: Dev PR
on:
  workflow_dispatch:
  push:
    paths:
      - .github/workflows/crossbow-test.yaml
      
permissions: 
  contents: read

jobs:        
  blub:
    runs-on: macos-14
      
    steps:
      - id: a
        run: |
          which python3
          which python
      - run: echo "${{ steps.a.outputs.b }}"
      - if: false
        uses: assignUser/stash/restore@main
        with:
          path: test_bin/
          key: test-sta/sh
          
      - if: false
        uses: assignUser/stash/save@main
        with:
          path: /usr/local/lib/node_modules
          key: test-stash-35/merge
